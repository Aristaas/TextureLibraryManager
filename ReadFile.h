#pragma once
#ifndef READFILE_H
#define READFILE_H


//this file should inbclude function prototypes and variable definition (Tiles of class)

class ReadFile
{
	private:
		
	public:
		//This is a constructor I think, it gets automatically called.  It should never contain a return.  We may need a disctructor.
		//THis is the constructor being called
		ReadFile();

	//protected:
	//private:
};
//notice there is a semicolon after the braces

#endif // !1